name: Deprecated - GitHub Pages deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "GitHub Pages deployment has been deprecated for this repo."
          echo "Use .github/workflows/deploy-firebase-hosting.yml instead."
